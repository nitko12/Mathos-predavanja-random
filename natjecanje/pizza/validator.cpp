#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    float r1 = inf.readDouble(0, 100, "r1");
    inf.readSpace();
    float r2 = inf.readDouble(0, 100, "r2");
    inf.readEoln();
    inf.readInt(0, 100, "n");
    inf.readEoln();
    inf.readEof();
}
